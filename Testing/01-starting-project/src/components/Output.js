import React from "react";
const Output = props =>{
    return <>{props.children}</> 
    // if component is not so complex we do not need to separate the test cases parent component test cases are enough
}